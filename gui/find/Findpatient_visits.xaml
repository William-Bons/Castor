<DataGrid x:Class="Castor.gui.find.Findpatient_visits"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:Castor.gui.find"
        mc:Ignorable="d" 
        d:DesignHeight="450" d:DesignWidth="800"
        ItemsSource="{Binding Visits}" AutoGenerateColumns="True" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" SelectedItem="{Binding Visit}"
        MouseDoubleClick="DataGrid_MouseDoubleClick"  
          
          >

    <DataGrid.Columns>
        <DataGridTextColumn Header="" Binding="{Binding Dep.Root.text}" />
        <DataGridTextColumn Header="" Binding="{Binding Dep.Root.Root.text}" />
        <DataGridTextColumn Header="" Binding="{Binding Dep.text}" />
        <DataGridTextColumn Header="" Binding="{Binding VPeriod}" />
        <DataGridTextColumn Header="" Binding="{Binding num}" />
        <DataGridTextColumn Header="" Binding="{Binding }" />
        <DataGridTextColumn Header="" Binding="{Binding }" />
    </DataGrid.Columns>
</DataGrid>
