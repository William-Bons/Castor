<Page x:Class="Castor.gui.pages.PlannsList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Castor.gui.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PlannsList">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ToolBar Grid.Row="0" Background="Transparent">
            <Button ToolTip="Фильтровать по выбранному врачу">
                <Image Source="/img/filter.png" Width="32" Height="32"  />
            </Button>
        </ToolBar>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Plannings, Mode=OneWay}" AutoGenerateColumns="True" IsReadOnly="True" MouseDoubleClick="PlanSelected" x:Name="PatientsTable" Language="RU">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Dic" Binding="{Binding Dictionary.description}" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>
