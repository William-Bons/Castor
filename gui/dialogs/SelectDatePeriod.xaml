<Window x:Class="Castor.gui.dialogs.SelectDatePeriod"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Castor.gui.dialogs"
        mc:Ignorable="d"
        Title="SelectDatePeriod" Width="350" Height="160" WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock  Grid.Row="0" Text="Дата начала периода" VerticalAlignment="Center"/>
        <DatePicker Grid.Row="0" Grid.Column="1" Margin="3" SelectedDate="{Binding SelectedStart}"/>
        <TextBlock  Grid.Row="1" Text="Дата конца периода" VerticalAlignment="Center"/>
        <DatePicker Grid.Row="1" Grid.Column="1" Margin="3" SelectedDate="{Binding SelectedEnd}"/>

    </Grid>
</Window>
