<Window x:Class="Castor.gui.login.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему Castor" Height="250" Width="350"
        WindowStartupLocation="CenterScreen" 
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Выберите пользователя или введите логин:" 
                   Style="{StaticResource BaseText}"
                   Margin="0,0,0,10"
                   FontWeight="SemiBold"/>

        <ComboBox x:Name="LoginComboBox"
                  Grid.Row="1"
                  Style="{StaticResource ModernComboBox}"
                  HorizontalAlignment="Stretch"
                  Margin="0,10,0,10"
                  IsEditable="False"
                  IsReadOnly="True"
                  SelectedValuePath="Id"
                  SelectionChanged="LoginComboBox_SelectionChanged">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding FullName}"
                       Foreground="{StaticResource Brush.TextMain}"
                       Padding="4,2"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>


        <!-- PasswordBox берёт HardPasswordBox из расширенного словаря -->
        <PasswordBox x:Name="PasswordBox"
                     Grid.Row="2"
                     Style="{StaticResource HardPasswordBox}"
                     Margin="0,0,0,20" 
                     ToolTip="Введите пароль"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена"
                    Width="80"
                    Margin="0,0,10,0"
                    Style="{StaticResource HardButton}"
                    Click="Cancel_Click"/>

            <Button Content="Войти"
                    Width="80"
                    Style="{StaticResource HardButton}"
                    IsDefault="True"
                    Click="Login_Click"/>
        </StackPanel>
    </Grid>
</Window>
